// Root layout — minimal shell for next-intl.
// All real layout (fonts, header, footer) is in app/[locale]/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
